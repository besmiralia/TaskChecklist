@page
@model TaskChecklist.Pages.Template.DetailsModel

@{
    ViewData["Title"] = "Template Details";
}

<h1>@(ViewData["Title"])</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.T03Template.TemplateName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.T03Template.TemplateName) <a asp-page="./Edit" asp-route-id="@Model.T03Template.Id">Rename</a>
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.T03Template.Active)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.T03Template.Active)
        </dd>
        
        <dt class="col-sm-2">
            Tasks
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                <tr>
                    <th>Task Name</th>
                    <th>Order</th>
                </tr>
                @foreach (var item in Model.T04Tasks.OrderBy(x=>x.TaskOrder).ToList())
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.TaskName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TaskOrder)
                        </td>
                        <td>
                            <a class="btn-primary btn-sm btn-default" href="./AddTasks?handler=move&move=up&id=@(Model.T03Template.Id)&taskId=@(item.Id)">Move up</a>
                            <a class="btn-primary btn-sm btn-default" href="./AddTasks?handler=move&move=down&id=@(Model.T03Template.Id)&taskId=@(item.Id)">Move down</a>
                            <a class="btn-primary btn-sm btn-danger" href="./AddTasks?handler=delete&id=@(Model.T03Template.Id)&taskId=@(item.Id)">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Index">Back to List</a>
</div>
